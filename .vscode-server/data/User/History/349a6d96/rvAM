FROM mariadb:latest
RUN apt-get update 
RUN apt-get install -y mariadb-client  
RUN mkdir -p /var/sql
COPY woodytoys.sql /var/sql

COPY sqlcmd.sh /usr/local/bin/

RUN chmod +x /usr/local/bin/sqlcmd.sh

ENTRYPOINT ["sqlcmd.sh"]




