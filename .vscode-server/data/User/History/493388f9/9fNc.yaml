
version: '3'

services:
  php:
    image:
    volumes:
    networks:
    command:
    port:
  web:
    image:
    volumes:
    networks:
    command:
    port:
  
  dns:
    image:
    volumes:
    networks:
    command:
    port:
  
  mariadb:
    image:
    volumes:
    networks:
    command:
    port:

networks:
