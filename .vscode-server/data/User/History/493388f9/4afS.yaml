
version: '3'

services:
  php:
    image: phpdo
    volumes:
    networks: Web_network
    command:
    port:
  web:
    image: ./web
    volumes: /home/tpuser/web/config/nginx.conf:/etc/nginx/nginx.conf
    networks: Web_network
    command:
    port: 80:80
  
  dns:
    image:
    volumes:
    networks: Web_network
    command:
    port:
  
  mariadb:
    image:
    volumes:
    networks: Web_network
    command: sudo apt install mariadb-client -y
    port:

networks:
  Web_network:

volumes:
