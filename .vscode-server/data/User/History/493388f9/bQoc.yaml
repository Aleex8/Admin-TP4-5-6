
version: '3'

services:
  php:
    image:
    volumes:
    networks: /home/tpuser/web/config/nginx.conf:/etc/nginx/nginx.conf
    command:
    port:
  web:
    image: ./web
    volumes:
    networks: Web_network
    command:
    port: 80:80
  
  dns:
    image:
    volumes:
    networks: Web_network
    command:
    port:
  
  mariadb:
    image:
    volumes:
    networks: Web_network
    command:
    port:

networks:
  Web_network:

volumes:
