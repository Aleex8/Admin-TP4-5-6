
version: '3'

services:
  php:
    image:
    volumes:
    networks:
    command:
  web:
    image:
    volumes:
    networks:
    command:
  
  dns:
    image:
    volumes:
    networks:
    command:

networks:
